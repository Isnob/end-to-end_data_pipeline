services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: crypto_db  # <-- Важно!
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"

  ingestor:
    build: .
    depends_on:
      - db