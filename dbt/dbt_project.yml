name: crypto_pipeline
version: "1.0.0"
config-version: 2

profile: crypto_pipeline

model-paths:
  - models

test-paths:
  - tests

clean-targets:
  - target
  - dbt_packages

models:
  crypto_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
